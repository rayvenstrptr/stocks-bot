# OS / editor
.DS_Store

# node
node_modules/

# atomic-write temp files (paper.mjs writes state via tmp+rename)
*.tmp

# one-off pipeline-test scratch
*_TEST*

# local daemon runtime (device-only; technicals-*.json ARE tracked so cloud premarket reads them)
.daemon-state.json
markets/daemon*.log
markets/*/state/quotes.json
